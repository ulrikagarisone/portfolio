<nav>
    <div class="container">
        <div class="nav-content">
            <a href="/" class="logo">UG</a>

            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/projects">Work</a></li>
                <li><a href="/design-translator">Insights</a></li>
                <li><a href="/about">About</a></li>
            </ul>
        </div>
    </div>
</nav>

<style>
    nav {
        position: sticky;
        top: 0;
        z-index: 100;
        background: rgba(10, 10, 10, 0.9);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        padding: 1rem 0;
    }

    .nav-content {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 1rem;
    }

    .logo {
        font-size: 1.25rem;
        font-weight: 900;
        color: var(--primary);
        transition: transform 0.3s ease;
    }

    .logo:hover {
        transform: scale(1.05);
    }

    .nav-links {
        list-style: none;
        display: flex;
        gap: 1rem;
        font-size: 0.875rem;
    }

    .nav-links a {
        color: var(--light);
        font-weight: 600;
        position: relative;
        padding: 0.5rem 0;
    }

    .nav-links a::after {
        content: "";
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 2px;
        background: var(--primary);
        transition: width 0.3s ease;
    }

    .nav-links a:hover::after {
        width: 100%;
    }
</style>
